use crate::math::vector::Vector3;

pub struct SailConfig {
    s_sail_wind_depend: f32,
    t_sail_wind_depend: f32,
    flex_speed: f32,
    max_sum_wind: f32,
    wind_angle_discrete: f32,
    max_turn_angle: f32,
    turn_step_angle: f32,
    rolling_speed: f32,
    wind_vector_t_incr: i32,
    wind_vector_t_add: i32,
    wind_vector_s_incr: i32,
    wind_vector_s_add: i32,
    wind_vector_quantity: i32,
    tex_quantity: i32,
    tex_num_common: i32,
    tex_num_english: i32,
    tex_num_triangle: i32,
    roll_z_value: f64,
    roll_z_delta: f64,
    roll_y_value: f64,
    roll_y_delta: f64,
    s_sail_roll_form: Vec<f64>,
    t_sail_roll_form: Vec<f64>,
    tr_form_mul: f64,
    f_wind_adding: f64,
    f_t_hole_flex_depend: f64,
    f_s_hole_flex_depend: f64,
    fall_s_sail_add_min: f64,
    fall_s_sail_add_rand: f64,
    fall_t_sail_add_min: f64,
    fall_t_sail_add_rand: f64,
    hole_depend: f64,
    ts: Vector3,
    fs: Vector3,
    ss: Vector3,
    diffuse_color: u32,
    ambient_color: u32,
    specular_color: u32,
    emissive_color: u32,
    material_power: f32,
    group_update_time: i32,
}
